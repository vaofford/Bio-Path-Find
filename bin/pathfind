#!/usr/bin/env perl

# PODNAME: pathfind

use v5.10; # for "say"

use strict;
use warnings;

use Try::Tiny;

use Bio::Path::Find::App::PathFind;

try {
  Bio::Path::Find::App::PathFind->new_with_options->run;
} catch {
  say STDERR $_;
};

